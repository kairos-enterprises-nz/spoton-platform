name: 03. Deploy SpotOn Staff (Production)

on:
  push:
    branches: [main, uat]
    paths: 
      - 'apps/staff-web/**'
      - 'deploy/**/staff.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Determine environment
        id: env
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "environment=production" >> $GITHUB_OUTPUT
            echo "image_tag=main-${{ github.sha }}" >> $GITHUB_OUTPUT
            echo "service_name=spoton-staff-live" >> $GITHUB_OUTPUT
          else
            echo "environment=uat" >> $GITHUB_OUTPUT
            echo "image_tag=uat-${{ github.sha }}" >> $GITHUB_OUTPUT
            echo "service_name=spoton-staff-uat" >> $GITHUB_OUTPUT
          fi
          echo "short_sha=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./apps/staff-web
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/spoton-platform-staff:${{ steps.env.outputs.image_tag }}
            ghcr.io/${{ github.repository_owner }}/spoton-platform-staff:${{ steps.env.outputs.environment }}-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          
      - name: Deployment Summary
        run: |
          echo "🚀 SpotOn Staff Deployment Complete!"
          echo ""
          echo "📦 Image: ghcr.io/${{ github.repository_owner }}/spoton-platform-staff:${{ steps.env.outputs.image_tag }}"
          echo "🌿 Environment: ${{ steps.env.outputs.environment }}"
          echo "🔖 Tag: ${{ steps.env.outputs.image_tag }}"
          echo ""
          echo "⚡ Portainer will automatically deploy this image via GitOps"
          echo "🔄 Expected in: ~5 minutes (Portainer polling interval)"
